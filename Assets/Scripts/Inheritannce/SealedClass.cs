using UnityEngine;

namespace Inheritance07
{
    //Sealed(봉인ㅋ) 클래스 : 최종 클래스, 더 이상 상속이 불가능
    public class SealedClass : MonoBehaviour
    {
        // Start is called once before the first execution of Update after the MonoBehaviour is created
        void Start()
        {

        }
    }

}
